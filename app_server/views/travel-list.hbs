<h1>{{title}}</h1>

<ul>
{{#each trips}}
    <li>
        <h3>{{this.name}}</h3>

        <img src="/images/{{this.image}}" alt="{{this.name}}" width="200">

        <p>{{this.description}}</p>
    </li>
{{/each}}
</ul>